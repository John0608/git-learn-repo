# Git 저장소 시작하기

## 1. 저장소 초기화

프로젝트 폴더로 이동한 뒤 Git 저장소를 만듭니다.

```bash
cd "대상 프로젝트 경로"
git init
```

실행하면 `.git` 폴더가 생성되고, 이 폴더부터 Git이 변경 사항을 추적합니다.

> [!INFO]
> 초기 브랜치 이름은 Git 설정에 따라 다를 수 있습니다. `main`인지 확인하려면 `git branch --show-current`을 실행합니다.

---

## 2. Git 추적에서 제외할 파일 설정

추적하지 않을 파일이나 폴더는 프로젝트 최상단의 `.gitignore`에 작성합니다.

```gitignore
.memsearch
```

`.gitignore`에 적은 항목은 아직 Git이 추적하지 않은 경우에만 이후 변경 사항에서 제외됩니다.

---

## 3. 변경 사항을 저장하기

- **스테이징**: 다음 커밋에 포함할 변경을 고르는 단계
- **커밋**: 스테이징한 변경을 Git 기록으로 저장하는 단계

```text
파일 수정 → git add → git commit
```

### 스테이징하기

```bash
# 파일 하나 추가
git add <파일명>

# 현재 폴더의 변경 사항 전체 추가
git add .

# 현재 상태 확인
git status
```

실수로 스테이징한 파일은 수정 내용은 유지한 채 스테이징에서만 뺄 수 있습니다.

```bash
git restore --staged <파일명>
```

### 커밋하기

```bash
git commit -m "커밋 메시지"
```

예시:

```bash
git commit -m "로그인 오류 수정"
```

---

## 4. 마지막 커밋 취소하기

수정한 파일은 유지하고 마지막 커밋만 취소합니다.

```bash
git reset --soft HEAD~1
```

스테이징도 취소하고 수정한 파일만 남깁니다.

```bash
git reset HEAD~1
```

수정한 파일까지 모두 버립니다. 복구가 번거로우므로 확실할 때만 사용합니다.

```bash
git reset --hard HEAD~1
```

## 5. 브랜치 사용하기

> 브랜치란, 하나의 프로젝트에서 여러 작업을 따로 진행하기 위해 나눈 작업 공간입니다.

| 명령어                             | 설명                                                                                 |
| ---------------------------------- | ------------------------------------------------------------------------------------ |
| `git branch`                       | 브랜치를 조회합니다                                                                  |
| `git branch -a`                    | 로컬 브랜치와 원격 브랜치를 조회합니다.                                              |
| `git branch <branch_name>`         | 브랜치를 생성합니다.                                                                 |
| `git switch <branch_name>`         | 해당 브랜치로 이동합니다.                                                            |
| `git switch -c <branch_name>`      | 해당 브랜치를 만든 후 이동합니다.                                                    |
| `git branch -d <branch_name>`      | 해당 브랜치를 제거합니다.                                                            |
| `git merge <branch_name>`          | 현재 브랜치에 선택한 브랜치의 변경 사항을 병합합니다.                                |
| `git merge --squash <branch_name>` | 선택한 브랜치의 여러 커밋 내용을 하나로 합쳐, 현재 브랜치에 새 커밋 하나로 만듭니다. |
| `git rebase <branch_name>`         | 현재 브랜치에서 만든 커밋을 선택한 브랜치의 최신 커밋 뒤에 다시 붙입니다.            |

## 6. 병합 충돌 해결하기

> [!TIP]
> 병합 충돌은 두 브랜치가 같은 파일의 같은 부분을 서로 다르게 수정했을 때 발생합니다. <br>
> Git이 어느 쪽 내용을 남겨야 할지 판단하지 못해, 직접 선택을 요청하는 상태입니다.

**충돌 난 파일**에는 아래와 같은 표시가 생깁니다.

```
<<<<<<< HEAD
현재 브랜치의 내용
=======
병합하려는 브랜치의 내용
> > > > > > > <병합 대상 브랜치 이름>
```

필요한 내용에 따라 한쪽만 남기거나, 두 내용을 합쳐서 수정합니다.

```bash
# 현재 브랜치 내용만 남길 때
현재 브랜치의 내용
```

```bash
# 병합하려는 브랜치 내용만 남길 때
병합하려는 브랜치의 내용
```

```bash
# 두 내용을 함께 반영할 때
현재 브랜치의 내용
병합하려는 브랜치의 내용
```

> [!WARNING]
> 수정이 끝나면 충돌 표시 줄을 모두 지워야 합니다.
