# Git 저장소 시작하기

## 0. 사용자 설정

Git은 커밋을 만들 때 작성자의 이름과 이메일을 기록합니다.

처음 Git을 사용한다면 이름과 이메일을 먼저 설정합니다.

### 사용자 정보 설정하기

```bash
# 이름을 전역으로 설정하기
git config --global user.name "이름"

# 이메일을 전역으로 설정하기
git config --global user.email "이메일"
```

### 설정 확인하기

```bash
# 이름 전역 설정 확인하기
git config --global user.name

# 이메일 전역 설정 확인하기
git config --global user.email
```

### 저장소별 사용자 정보 설정하기

> [!TIP]
> 회사 저장소처럼 현재 프로젝트에서만 다른 이름이나 이메일을 사용하려면 `--global` 없이 설정합니다.

```bash
git config user.name "회사 이름"
git config user.email "company@example.com"
```

## 1. 저장소 초기화

프로젝트 폴더로 이동한 뒤 Git 저장소를 만듭니다.

```bash
cd "대상 프로젝트 경로"
git init
```

실행하면 `.git` 폴더가 생성되고, 이 폴더부터 Git이 변경 사항을 추적합니다.

> [!INFO]
> 초기 브랜치 이름은 Git 설정에 따라 다를 수 있습니다. `main`인지 확인하려면 `git branch --show-current`을 실행합니다.

---

## 2. Git 추적에서 제외할 파일 설정

추적하지 않을 파일이나 폴더는 프로젝트 최상단의 `.gitignore`에 작성합니다.

```gitignore
.memsearch
```

`.gitignore`에 적은 항목은 아직 Git이 추적하지 않은 경우에만 이후 변경 사항에서 제외됩니다.

---

## 3. 변경 사항을 저장하기

> [!TIP]
> **스테이징**: 다음 커밋에 포함할 변경을 고르는 단계  
> **커밋**: 스테이징한 변경을 Git 기록으로 저장하는 단계

```text
파일 수정 → git add → git commit
```

### 스테이징하기

```bash
# 파일 하나 추가
git add <파일명>

# 현재 폴더의 변경 사항 전체 추가
git add .

# 현재 상태 확인
git status
```

실수로 스테이징한 파일은 수정 내용은 유지한 채 스테이징에서만 뺄 수 있습니다.

```bash
git restore --staged <파일명>
```

### 커밋하기

```bash
git commit -m "커밋 메시지"
```

예시:

```bash
git commit -m "로그인 오류 수정"
```

---

## 4. 마지막 커밋 취소하기

수정한 파일은 유지하고 마지막 커밋만 취소합니다.

```bash
git reset --soft HEAD~1
```

스테이징도 취소하고 수정한 파일만 남깁니다.

```bash
git reset HEAD~1
```

수정한 파일까지 모두 버립니다. 복구가 번거로우므로 확실할 때만 사용합니다.

```bash
git reset --hard HEAD~1
```

## 5. 브랜치 사용하기

> [!TIP]
> 브랜치란, 하나의 프로젝트에서 여러 작업을 따로 진행하기 위해 나눈 작업 공간입니다.

| 명령어                             | 설명                                                                                                                           |
| ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| `git branch`                       | 브랜치를 조회합니다                                                                                                            |
| `git branch -a`                    | 로컬 브랜치와 원격 브랜치를 조회합니다.                                                                                        |
| `git branch <브랜치-이름>`         | 브랜치를 생성합니다.                                                                                                           |
| `git switch <브랜치-이름>`         | 해당 브랜치로 이동합니다.                                                                                                      |
| `git switch -c <브랜치-이름>`      | 해당 브랜치를 만든 후 이동합니다.                                                                                              |
| `git branch -d <브랜치-이름>`      | 해당 브랜치를 제거합니다.                                                                                                      |
| `git merge <브랜치-이름>`          | 현재 브랜치에 선택한 브랜치의 변경 사항을 병합합니다.                                                                          |
| `git merge --squash <브랜치-이름>` | 선택한 브랜치의 여러 커밋 내용을 하나로 합쳐, 현재 브랜치에 새 커밋 하나로 만듭니다. <br> `git commit`을 해야 최종 병합됩니다. |
| `git rebase <브랜치-이름>`         | 현재 브랜치에서 만든 커밋을 선택한 브랜치의 최신 커밋 뒤에 다시 붙입니다.                                                      |

## 6. 병합 충돌 해결하기

> [!TIP]
> 병합 충돌은 두 브랜치가 같은 파일의 같은 부분을 서로 다르게 수정했을 때 발생합니다. <br>
> Git이 어느 쪽 내용을 남겨야 할지 판단하지 못해, 직접 선택을 요청하는 상태입니다.

**충돌 난 파일**에는 아래와 같은 표시가 생깁니다.

```
<<<<<<< HEAD
현재 브랜치의 내용
=======
병합하려는 브랜치의 내용
> > > > > > > <병합 대상 브랜치 이름>
```

#### 필요한 내용에 따라 한쪽만 남기거나, 두 내용을 합쳐서 수정합니다.

> [!WARNING]
> 수정이 끝나면 충돌 표시 줄을 모두 지워야 합니다.

```bash
# 현재 브랜치 내용만 남길 때
현재 브랜치의 내용
```

```bash
# 병합하려는 브랜치 내용만 남길 때
병합하려는 브랜치의 내용
```

```bash
# 두 내용을 함께 반영할 때
현재 브랜치의 내용
병합하려는 브랜치의 내용
```

#### 병합을 중단하고 돌아가려면, 방식에 따라 아래와 같이 진행하세요.

```bash
# 일반 merge를 중단하고, merge 전 상태로 돌아가려면
git merge --abort
```

```bash
# squash merge를 중단하고, squash merge 전 상태로 돌아가려면
git reset --merge
```

```bash
# rebase를 중단하고, rebase 시도 전 상태로 돌아가려면
git rebase --abort
```

## 7. 변경사항 비교하기

| 명령어                          | 설명                                                                   |
| ------------------------------- | ---------------------------------------------------------------------- |
| `git diff`                      | 아직 스테이징 하지 않은 변경 내용을 확인합니다.                        |
| `git diff --staged`             | 스테이징한 변경 내용을 확인합니다.                                     |
| `git diff HEAD`                 | 마지막 커밋 이후의 모든 변경 내용을 확인합니다.                        |
| `git diff <파일명>`             | 특정 파일의 변경 내용을 확인합니다.                                    |
| `git diff main...<대상-브랜치>` | `main` 에서 갈라진 뒤 `<대상 브랜치>`에 추가된 변경 내용을 확인합니다. |
| `git diff --check`              | 줄 끝 공백 등 공백 오류를 확인합니다.                                  |

## 8. 커밋 기록 확인하기

| 명령어              | 설명                                                   |
| ------------------- | ------------------------------------------------------ |
| `git log`           | 커밋 기록을 자세히 확인합니다.                         |
| `git log --oneline` | 커밋 기록을 한 줄씩 간단히 확인합니다.                 |
| `git show HEAD`     | 가장 최근 커밋의 변경 내용을 확인합니다.               |
| `git reflog`        | 브랜치 이동, 커밋 취소 등 로컬 작업 기록을 확인합니다. |

## 9. 원격 저장소

> [!NOTE]
> 원격 저장소는 내 컴퓨터 밖, GitHub 같은 온라인 서비스에 있는 프로젝트 저장소입니다.

| 명령어                                     | 설명                                                                |
| ------------------------------------------ | ------------------------------------------------------------------- |
| `git remote add origin <원격-저장소-주소>` | 원격 저장소 주소로 내 저장소를 연결합니다.                          |
| `git remote -v`                            | 연결된 원격 저장소를 확인합니다.                                    |
| `git push`                                 | 현재 브랜치의 커밋을 원격 저장소에 올립니다.                        |
| `git push -u origin <대상-브랜치>`         | 처음 원격 저장소에 올릴 때, 현재 브랜치와 원격 브랜치를 연결합니다. |
| `git pull`                                 | 원격 저장소의 변경사항을 가져와 현재 브랜치에 반영합니다.           |

## 10. 작업 내용 임시 보관하기

> [!NOTE]
> `git stash`는 아직 커밋하지 않은 작업 내용을 잠시 보관하는 명령어입니다.
> 브랜치를 바꾸거나 원격 저장소의 변경 사항을 먼저 받아와야 할 때 사용합니다.

| 명령어            | 설명                                                            |
| ----------------- | --------------------------------------------------------------- |
| `git stash`       | 현재 변경 사항을 임시 보관합니다.                               |
| `git stash -u`    | 추적하지 않는 새 파일까지 함께 임시 보관합니다.                 |
| `git stash list`  | 임시 보관한 작업 목록을 확인합니다.                             |
| `git stash pop`   | 가장 최근에 보관한 작업을 다시 적용하고 목록에서 제거합니다.    |
| `git stash apply` | 가장 최근에 보관한 작업을 다시 적용하지만 목록에는 남겨 둡니다. |
| `git stash drop`  | 가장 최근에 보관한 작업을 삭제합니다.                           |

## 11. 수정 내용 되돌리기

> [!warning]
> `git restore <파일명>`은 수정 내용을 지웁니다. 실행 전 `git diff <> 파일명` 으로 확인하세요

| 명령어                          | 설명                                                               |
| ------------------------------- | ------------------------------------------------------------------ |
| `git restore <파일명>`          | 스테이징하지 않은 파일의 수정내용을 마지막 커밋 상태로 되돌립니다. |
| `git restore --staged <파일명>` | 스테이징만 취소하고 파일 수정내용은 유지합니다.                    |
